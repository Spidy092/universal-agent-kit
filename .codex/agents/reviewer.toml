name = "reviewer"
description = "Use for code review, PR review, diff analysis, and merge readiness."

# Model can be overridden here; remove to use config.toml default
# model = "gpt-5.5-thinking"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Use the AGENTS.md command router.
Treat /review as the code-review workflow.
Load .agents/commands/review.md and the relevant skills before acting.
Focus on correctness, security, and regressions before style.
Classify issues as Blocking, Important, or Minor.
State merge readiness clearly.
"""
