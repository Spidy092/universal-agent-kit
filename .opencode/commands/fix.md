# /fix

Read `AGENTS.md`, then use `.agents/commands/fix.md` and `.agents/skills/rtk-prompting/SKILL.md`.

Arguments: `$ARGUMENTS` is the task to implement or repair.

Auto-add `debugging`, `security-audit`, or `frontend-ui` when the task text matches those domains. Implement the smallest safe change and verify it.
