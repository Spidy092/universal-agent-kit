# /mode

Read `AGENTS.md`, then use `.agents/commands/mode.md`.

Arguments: `$ARGUMENTS` should be a mode name such as `strict`, `frontend`, `backend`, `security`, `production`, `fast`, `research`, `review`, or `reset`.

Maintain active mode behavior for the session. Combine modes conservatively; strict, security, and production override fast when they conflict.
