# /refactor

Read `AGENTS.md`, then use `.agents/commands/refactor.md` and `.agents/skills/refactor/SKILL.md`.

Arguments: `$ARGUMENTS` may describe the cleanup scope.

Preserve behavior, avoid unrelated rewrites, keep the diff focused, and verify.
