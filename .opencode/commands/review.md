# /review

Read `AGENTS.md`, then use `.agents/commands/review.md` and `.agents/skills/code-review/SKILL.md`.

Arguments: `$ARGUMENTS` may name a diff, PR, branch, file, or review scope.

Lead with Blocking, Important, Minor, Missing tests, and Merge readiness.
