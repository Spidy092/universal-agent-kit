# /security

Read `AGENTS.md`, then use `.agents/commands/security.md` and `.agents/skills/security-audit/SKILL.md`.

Arguments: `$ARGUMENTS` may define a security review or fix scope.

Do not expose secrets. Check auth, authz, secrets, injection, rate limits, CORS, uploads, payments, webhooks, and permissions as relevant.
