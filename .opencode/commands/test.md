# /test

Read `AGENTS.md`, then use `.agents/commands/test.md` and `.agents/skills/testing/SKILL.md`.

Arguments: `$ARGUMENTS` may describe test scope, coverage goals, failing tests, or regression cases.

Add focused tests where useful and report exact commands and results.
