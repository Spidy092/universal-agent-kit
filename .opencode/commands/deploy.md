# /deploy

Read `AGENTS.md`, then use `.agents/commands/deploy.md` and `.agents/skills/devops-deploy/SKILL.md`.

Arguments: `$ARGUMENTS` may describe deploy, CI/CD, Docker, Nginx, PM2, SSL, AWS, VPS, logs, or rollback work.

Ask before destructive or production-impacting actions. Include health checks, logs, rollback, and environment risks.
