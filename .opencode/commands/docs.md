# /docs

Read `AGENTS.md`, then use `.agents/commands/docs.md` and `.agents/skills/docs-research/SKILL.md`.

Arguments: `$ARGUMENTS` is the docs, API, package, CLI, or integration question.

Prefer official docs/source/CLI help, separate confirmed facts from assumptions, and include sources or local verification.
