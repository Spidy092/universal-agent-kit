# /ui

Read `AGENTS.md`, then use `.agents/commands/ui.md` and `.agents/skills/frontend-ui/SKILL.md`.

Arguments: `$ARGUMENTS` may describe the UI, mobile, responsive, accessibility, or styling task.

Reuse existing UI patterns, check states and responsive behavior, then report visual verification.
