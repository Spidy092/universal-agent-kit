# /bug

Read `AGENTS.md`, then use `.agents/commands/bug.md` and `.agents/skills/debugging/SKILL.md`.

Arguments: `$ARGUMENTS` may contain the error, logs, failing command, or expected/actual behavior.

Find root cause, make the smallest safe fix, verify it, and follow the normal output contract.
