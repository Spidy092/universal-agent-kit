# backend mode

Session behavior:
- Check validation, error handling, logging, and API compatibility.
- Preserve existing API contracts unless the user explicitly requests a breaking change.
- Mention database and migration impact.
- Consider auth, permissions, rate limits, and operational failure modes.
- Include exact test commands.
