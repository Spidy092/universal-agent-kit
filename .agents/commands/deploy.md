# /deploy

Aliases: `/devops`

Skills: `devops-deploy`

Use for deployment, CI/CD, Docker, Nginx, PM2, systemd, SSL, VPS, AWS, logs, health checks, rollback, and production operations.

Required behavior:
- Read `AGENTS.md` first.
- Load `.agents/skills/devops-deploy/SKILL.md`.
- Consider `security-audit` when secrets, auth, public network exposure, or production config are involved.
- Ask before destructive or production-impacting actions.
- Include rollback, logs, health checks, and environment risks.

Output format:
- Summary
- What I found
- Fix / Changes
- Files changed
- Commands to run
- Verification
- Risks / Edge cases
